[package]
name = "stockweave"
version = "0.1.0"
description = "StockWeave strategy accounts and lifecycle (Phase 4)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "stockweave"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
